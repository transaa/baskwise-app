"""PriceWise — receipt-powered grocery & household price tracker.

A demo of the "flip the data problem" approach: instead of crawling retailers,
users bring their own receipts. The app parses them into line items, normalizes
and categorizes each item, and builds a personal (and, at scale, crowdsourced)
real-world price database.
"""

__version__ = "0.1.0"
